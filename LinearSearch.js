//WRITE A FUNCTION CALLED LINEARSEARCH WHICH ASSPETS AN ARRAY AND A VALUE, AND RETURNS THE INDEX AT WHICH THE VALUE EXISTS. IF TYHE VALUE DOES NOT EXIST IN THE ARRAY, RETURN -1




function linearSearch(arr, val){

    let index = arr.indexOf(val)
  
  
   return index
    
   // index > -1 ? return index : console.log(false) 
    
    
  }
  

  //attempt without using the indexOF